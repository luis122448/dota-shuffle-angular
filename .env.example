# Copy this file to .env and fill in the values before running locally.
# Never commit .env — it must be listed in .gitignore.

# Backend API URL (HTTP — used by Nginx reverse proxy)
API_URL_METRICS=
# Backend WebSocket URL (WS/WSS — used by Nginx reverse proxy)
WS_URL_METRICS=
